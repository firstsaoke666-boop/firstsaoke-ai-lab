[project]
name = "firstsaoke-config"
version = "0.1.0"
description = "Configuration interfaces for firstsaoke-ai-lab"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.30,<0.12"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "firstsaoke_config"
